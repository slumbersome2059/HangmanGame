public class Game {
    private int attempt;
    public static final int drawCharacters = 8;
    public Game(){
        attempt = 0;
        while (attempt < drawCharacters){
            //l = UI.getInput()
            //ind = word.contains(l)
            //if(ind != -1){
            // UI.printOut(ind)
            // }else{
            // UI.printHang(attempt)
            // }
            attempt += 1;
        }
    }
}
